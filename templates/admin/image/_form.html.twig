{{ form_start(form) }}
{{ form_row(form.name) }}
 {% if  images.image != null %}
     <div style="width: 150px; flex: none; margin: 5px">
         <img src="/upload/{{ images.image }}" alt="image" width="100px" height="auto">
         <a href="{{ path('admin.image.delete.image', {'id': images.id}) }}" class="btn-danger mt2 mb2">
             Supprimer
         </a>
     </div>
 {% endif %}
{{ form_row(form.image) }}
<button type="submit" class="btn-primary ">{{ button | default('Enregistrer') }}</button>
{{ form_end(form) }}